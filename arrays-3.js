// оздай массив с десятью элементами числового типа. Три числа должны быть меньше нуля.
// Ниже напиши цикл for, который выведет все положительные числа в консоль.
const array =[25,36,-17,28,-9,38,-5,89,34,11];
for(let i=0; i<array.length;i++){
	if(array[i]<0){
		console.log(array[i]);
	}
}